/**
 * Types User - Ré-exportés depuis le domaine partagé users/
 * Ce fichier existe pour compatibilité rétroactive
 */
export type {
  User,
  CreateUserRequest,
  UpdateUserRequest,
} from "../../users/types/user";
