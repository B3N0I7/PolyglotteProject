/**
 * Types spécifiques à l'affichage des mots
 * Les types de domaine (Word) sont dans features/words/types
 * Ce fichier peut contenir des types UI spécifiques comme filtres, tri, pagination, etc.
 */

// Réexporter Word pour compatibilité temporaire
export type { Word } from "../../words/types/word";
